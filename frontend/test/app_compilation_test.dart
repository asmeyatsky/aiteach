import 'package:flutter_test/flutter_test.dart';

void main() {
  test('App compiles without errors', () {
    // This test will pass if the app compiles without errors
    expect(true, isTrue);
  });
}